{
      "version": 2,
      "builds": [
            { "src": "*.js", "use": "@vercel/static" }
      ],
      "routes": [
            { "src": "/(.*)", "dest": "/" }
      ],
      "rewrites": [
            { "source": "/server(.*)", "destination": "/server" }
      ],
      "requireStack": [ "/vercel/path0/server/app.js", "/vercel/path0/server/server.js" ]
}